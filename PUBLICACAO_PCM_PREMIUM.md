# Publicação do Painel PCM Premium\n\nA versão premium foi integrada ao build existente sem remover os scripts e dados operacionais legados. O arquivo raiz `index.html` agora aponta para os assets em `static/pcm-app`, e `tools/build-dist.cjs` inclui esses assets no pacote `dist`.\n\nO Cloudflare deve continuar usando:\n\n- Build command: `npm run build`\n- Output directory, quando aplicável: `dist`\n\nA base operacional continua sendo carregada pelos arquivos públicos do GitHub configurados no dashboard.\n